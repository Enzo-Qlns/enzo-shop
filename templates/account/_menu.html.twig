<div class="card">
    <div class="card-body">
        <div class="d-flex flex-column align-items-center text-center">
            <img src="https://bootdey.com/img/Content/avatar/avatar7.png" alt="Admin" class="rounded-circle" width="150">
            <div class="mt-3">
                <h4>{{ app.user.firstname }} {{ app.user.lastname }}</h4>
                <p class="text-muted font-size-sm">{{ app.user.email }}</p>
            </div>
        </div>
        <hr>
        <ul class="list-group list-group-flush">
            <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                <a href="{{ path('app_account') }}" class="text-decoration-none text-dark">
                    <i data-feather="user"></i> Mon compte
                </a>
            </li>
            <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                <a href="{{ path('app_modify_password') }}" class="text-decoration-none text-dark">
                    <i data-feather="lock"></i> Modifier mon mot de passe
                </a>
            </li>
            <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                <a href="{{ path('app_addresses') }}" class="text-decoration-none text-dark">
                    <i data-feather="map-pin"></i> Mes adresses
                </a>
            </li>
            <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                <a href="{{ path('app_orders') }}" class="text-decoration-none text-dark">
                    <i data-feather="shopping-bag"></i> Mes commandes
                </a>
            </li>
            <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                <a href="{{ path('app_cart') }}" class="text-decoration-none text-dark">
                    <i data-feather="shopping-cart"></i> Mon panier
                </a>
            </li>
            <li class="list-group-item d-flex justify-content-between align-items-center flex-wrap">
                <a href="{{ path('app_logout') }}" class="text-decoration-none text-danger">
                    <i data-feather="log-out"></i> Déconnexion
                </a>
            </li>
        </ul>
    </div>
</div> 